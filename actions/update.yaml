---
name: "update"
runner_type: "python-script"
description: "Update a new entry in a zendesk tickets"
enabled: true
entry_point: "update.py"
parameters:
  subject:
    type: "string"
    description: "Subject of the ticket to search for "
    required: false 
  ticket_id:
    type: "string"
    description: "ID of the ticket to search for "
    required: false 
